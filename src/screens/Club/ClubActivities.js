import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export default function ActivityHistory() {
    return (
        <View style={styles.container}>
            <Text>ActivityHistory</Text>
        </View>
    )
};

const styles = StyleSheet.create({
    container: {
      flex: 1,
    },
  });